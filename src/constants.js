const LOCK_FILE = './cache/lock';
const META_FILE = './cache/meta';
const HODL_FILE = './cache/hodl';
const SNAP_FILE = './cache/snap';
const BIFI_FILE = './cache/bifi';

module.exports = {
  LOCK_FILE,
  META_FILE,
  HODL_FILE,
  SNAP_FILE,
  BIFI_FILE,
}